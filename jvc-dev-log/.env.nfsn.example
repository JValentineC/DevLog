# -- Server ----------------------------------------------------
NODE_ENV=production
PORT=80
CORS_ORIGIN=https://jvalentinec.github.io

# -- Database (MySQL / MariaDB on NFSN) -----------------------
DATABASE_URL="mysql://icstars_log_admin:YOUR_ENCODED_PASSWORD@icstarslog.db:3306/icstarslog"

# -- Auth ------------------------------------------------------
JWT_SECRET="REPLACE-WITH-A-LONG-RANDOM-STRING"
